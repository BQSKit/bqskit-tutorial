[package]
name = "ppm_transpiler"
version = "0.1.0"
edition = "2021"

[lib]
name = "ppm_transpiler"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.29", features = ["extension-module"] }
